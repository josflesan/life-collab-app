import 'package:flutter/material.dart';

class NoGroupScreen extends StatelessWidget {
  const NoGroupScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("No Group Screen"),
      ),
    );
  }
}
